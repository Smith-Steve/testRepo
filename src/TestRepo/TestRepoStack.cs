using Amazon.CDK;
using Constructs;

namespace TestRepo
{
    public class TestRepoStack : Stack
    {
        internal TestRepoStack(Construct scope, string id, IStackProps props = null) : base(scope, id, props)
        {
            // The code that defines your stack goes here
        }
    }
}
